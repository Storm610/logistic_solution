
Процедура ПередЗаписью(Отказ)
	
	Если ЭтоНовый() Тогда
		
		
		Если Тележка.ПолучитьЯзык(client_id) = "EN" тогда
			ТекстСообщения = "You have successfully registered, now you can place orders /neworder";
		иначе
			ТекстСообщения = "Вы зарегистрированы, теперь можно делать заказы /neworder";
		конецесли;
		
		Обработки.Telegram.ОтправитьСообщение(client_id, ТекстСообщения);
		
	КонецЕсли;
	
КонецПроцедуры
	
	
